# Gate 2 — Implementation

Pendiente. Se abre al cerrar el gate anterior.

Previsto: workflow `build` (publica `bragi-sync`), workflow `ci` (compose config, shellcheck,
validación Mermaid, gitleaks), `network.xml` de referencia con KnownProxies.
